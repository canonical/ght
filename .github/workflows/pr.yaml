name: PR check
on: pull_request

env:
    CREDENTIALS: ${{ secrets.CREDENTIALS }}

jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Run ght tests
              run: yarn dev doctor
